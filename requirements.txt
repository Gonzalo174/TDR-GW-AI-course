# Entorno `TDR_GW` (conda), python 3.12.11.
#
#     conda create -n TDR_GW python=3.12
#     conda activate TDR_GW
#     pip install -r requirements.txt
#
# Versiones con las que se corrieron las 35 pruebas de `comun/tests/` y la carga
# completa de la base. La maquina de origen tiene glibc 2.27, que limita pyarrow
# a <=15 y arrastra un downgrade de numpy: por eso la base va en csv.gz y no en
# parquet.
pandas==3.0.5
numpy==2.5.3
scipy==1.18.1
scikit-learn==1.9.0
matplotlib==3.11.1
statsmodels==0.15.0
jupyter==1.1.1
notebook==7.6.2
nbconvert==7.17.1
pytest==9.1.1
