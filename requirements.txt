# Entorno `TDR_GW` (conda), python 3.12.11.
#
#     conda create -n TDR_GW python=3.12
#     conda activate TDR_GW
#     pip install -r requirements.txt
#
# Las versiones con las que pasan las 35 pruebas de `comun/tests/` y carga la
# base completa. La lista sale de recorrer todos los `import` del repositorio
# -notebooks incluidos- y verificar que cada uno resuelve en el entorno.
#
# La maquina de origen tiene glibc 2.27, que limita pyarrow a <=15 y arrastra un
# downgrade de numpy: por eso la base va en csv.gz y no en parquet.

# --- lo que usa el analisis ---
pandas==3.0.5
numpy==2.5.3
scipy==1.18.1
scikit-learn==1.9.0
statsmodels==0.15.0
matplotlib==3.11.1
seaborn==0.13.2
networkx==3.6.1

# --- para correr los notebooks y los tests ---
jupyter==1.1.1
notebook==7.6.2
nbconvert==7.17.1
pytest==9.1.1
